Step 1. Create directory inside kartify directory named after new app, let it be app_name

Step 2. run
```./manage.py startapp app_name ./kartify/app_name```

Step 3. add "kartify.app_name" in LOCAL_APPS of config/settings/base.py